# Godot 4 cache/imported files
.godot/

# Android build artifacts (gradle build dir, if ever enabled)
android/

# Export presets hold keystore paths/passwords once Android export is set up.
# Keep them out of the repo; recreate per-machine (see README).
export_presets.cfg
